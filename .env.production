# Vite carga este archivo automáticamente en `npm run build` (modo production).
# El build de GitHub Pages apunta al backend desplegado.
# En `npm run dev` (desarrollo) se usa el default http://localhost:8000.
VITE_API_BASE_URL=https://godeye.whatbrainy.com
