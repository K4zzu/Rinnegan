# Caddy rutea por subdominio y saca/renueva HTTPS (Let's Encrypt) solo.
# Sustituye TUDOMINIO.com y el email antes de levantar.

{
	email tu-email@ejemplo.com
}

n8n.TUDOMINIO.com {
	reverse_proxy n8n:5678
}

api.TUDOMINIO.com {
	reverse_proxy backend:8000
}
